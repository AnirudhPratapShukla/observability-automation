# Part 3 - Question 2: Elasticsearch Watcher

## Overview

This solution uses Elasticsearch Watcher to monitor rbcapp1 service status
data and trigger an alert when a service is reported as DOWN.

The Watcher configuration is stored in:

```text
watcher.json
