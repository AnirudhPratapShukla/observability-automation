# Part 5 - Question 3: Kibana Dashboard

## Overview

The `RBAPP1 Service Monitoring` dashboard is designed to provide a real-time
view of rbcapp1 application health using service status data stored in
Elasticsearch.

The dashboard uses the `service-status-*` index pattern.

## Data Source

Elasticsearch index pattern:

```text
service-status-*
